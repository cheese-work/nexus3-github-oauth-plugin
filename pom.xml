<?xml version="1.0" encoding="UTF-8"?>
<!--
  Nexus 3.93.2+ port of nexus3-github-oauth-plugin.

  Nexus 3.71+ replaced the Karaf/OSGi runtime with a Spring Boot runtime.
  Plugins are now plain jars discovered by Spring component scanning when
  dropped into $NEXUS_HOME/deploy/. This pom mirrors the structure of the
  bundled org.sonatype.nexus.common.components:nexus-default-role-plugin,
  inheriting version management from the published Nexus components parent.
-->
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">

  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>org.sonatype.nexus.common.components</groupId>
    <artifactId>nexus-common-components-parent</artifactId>
    <version>3.93.2-01</version>
    <relativePath/>
  </parent>

  <groupId>com.larscheidschmitzhermes</groupId>
  <artifactId>nexus3-github-oauth-plugin</artifactId>
  <version>3.93.2-01</version>

  <name>${project.groupId}:${project.artifactId}</name>
  <description>GitHub OAuth authentication realm for Sonatype Nexus Repository 3.93+ (Spring runtime)</description>

  <properties>
    <maven.compiler.release>21</maven.compiler.release>
  </properties>

  <dependencies>
    <!-- Nexus plugin SPI and security (managed versions from the parent) -->
    <dependency>
      <groupId>org.sonatype.nexus.common.components</groupId>
      <artifactId>nexus-plugin-api</artifactId>
      <scope>provided</scope>
    </dependency>
    <dependency>
      <groupId>org.sonatype.nexus.common.components</groupId>
      <artifactId>nexus-security</artifactId>
      <scope>provided</scope>
    </dependency>

    <!-- Shiro realm SPI (provided by Nexus runtime) -->
    <dependency>
      <groupId>org.apache.shiro</groupId>
      <artifactId>shiro-core</artifactId>
      <scope>provided</scope>
    </dependency>

    <!-- HTTP + JSON for the GitHub API client (bundled into the plugin jar) -->
    <dependency>
      <groupId>org.apache.httpcomponents</groupId>
      <artifactId>httpclient</artifactId>
    </dependency>
    <dependency>
      <groupId>com.fasterxml.jackson.core</groupId>
      <artifactId>jackson-databind</artifactId>
    </dependency>

    <!-- Guava cache for principal caching (bundled into the plugin jar) -->
    <dependency>
      <groupId>com.google.guava</groupId>
      <artifactId>guava</artifactId>
    </dependency>

    <!-- Test -->
    <dependency>
      <groupId>junit</groupId>
      <artifactId>junit</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.hamcrest</groupId>
      <artifactId>hamcrest-core</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.mockito</groupId>
      <artifactId>mockito-core</artifactId>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-compiler-plugin</artifactId>
        <configuration>
          <release>21</release>
        </configuration>
      </plugin>
      <!-- Shade external deps (httpclient/jackson/guava) into the plugin jar so
           the dropped-in jar is self-contained, while keeping Nexus/Shiro classes
           provided by the runtime. -->
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-shade-plugin</artifactId>
        <executions>
          <execution>
            <phase>package</phase>
            <goals><goal>shade</goal></goals>
            <configuration>
              <createDependencyReducedPom>false</createDependencyReducedPom>
              <artifactSet>
                <includes>
                  <include>org.apache.httpcomponents:httpclient</include>
                  <include>org.apache.httpcomponents:httpcore</include>
                  <include>commons-logging:commons-logging</include>
                  <include>commons-codec:commons-codec</include>
                  <include>com.fasterxml.jackson.core:jackson-databind</include>
                  <include>com.fasterxml.jackson.core:jackson-core</include>
                  <include>com.fasterxml.jackson.core:jackson-annotations</include>
                  <include>com.google.guava:guava</include>
                  <include>com.google.guava:failureaccess</include>
                  <include>com.google.code.findbugs:jsr305</include>
                </includes>
              </artifactSet>
            </configuration>
          </execution>
        </executions>
      </plugin>
    </plugins>
  </build>
</project>
